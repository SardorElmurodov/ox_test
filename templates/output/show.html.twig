{% extends 'base.html.twig' %}

{% block title %}Output{% endblock %}

{% block body %}
    <h1>Output</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ output.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ output.quantity }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ output.amount }}</td>
            </tr>
            <tr>
                <th>Output_date</th>
                <td>{{ output.outputDate ? output.outputDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('output_index') }}">back to list</a>

    <a href="{{ path('output_edit', {'id': output.id}) }}">edit</a>

    {{ include('output/_delete_form.html.twig') }}
{% endblock %}
